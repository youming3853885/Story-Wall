# Firebase 設定指南

## 🔧 必要的Firebase設定

### 1. Firestore Database 規則設定

請在Firebase控制台中設定以下Firestore規則：

1. 前往 Firebase 控制台
2. 選擇您的專案 `story-wall-7af82`
3. 左側選單 → Firestore Database → 規則
4. 將以下規則貼上：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允許讀取和寫入失物資料
    match /lost_items/{document} {
      allow read, write: if true;
    }
    
    // 允許讀取和寫入已歸還資料
    match /returned_items/{document} {
      allow read, write: if true;
    }
  }
}
```

### 2. Firebase Storage 規則設定

請在Firebase控制台中設定以下Storage規則：

1. 前往 Firebase 控制台
2. 選擇您的專案 `story-wall-7af82`
3. 左側選單 → Storage → 規則
4. 將以下規則貼上：

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 允許讀取和上傳失物圖片
    match /lost-items-images/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
```

### 3. 建立Storage Bucket

1. 在Firebase控制台中
2. 左側選單 → Storage
3. 點擊「開始使用」
4. 選擇「以測試模式開始」
5. 選擇地區（建議選擇 asia-east1 台灣）

## 🚀 遷移步驟

### 步驟1：執行資料遷移

1. 開啟 `firebase-migration.html`
2. 點擊「測試連接」確認所有服務正常
3. 點擊「開始遷移」執行完整遷移

### 步驟2：測試新系統

1. 開啟 `firebase-test.html`
2. 確認右上角顯示「✅ Firebase 已連接」
3. 測試以下功能：
   - 查看現有失物
   - 上傳新失物
   - 播放故事
   - 找到主人功能

### 步驟3：切換到Firebase版本

當測試確認無誤後：

1. 將 `script-firebase.js` 重命名為 `script.js`（備份原檔案）
2. 將 `firebase-test.html` 的內容複製到 `index.html`
3. 確認所有功能正常運作

## 📊 遷移檢查清單

- [ ] Firebase專案已建立
- [ ] Firestore Database已啟用
- [ ] Firebase Storage已啟用
- [ ] Firestore規則已設定
- [ ] Storage規則已設定
- [ ] 資料遷移已完成
- [ ] 失物資料顯示正常
- [ ] 圖片上傳功能正常
- [ ] 故事播放功能正常
- [ ] 找到主人功能正常
- [ ] 管理員功能正常

## 🔍 常見問題

### Q: 遷移後看不到資料？
A: 檢查Firestore規則是否正確設定，確認允許讀取權限。

### Q: 圖片上傳失敗？
A: 檢查Storage規則是否正確設定，確認允許寫入權限。

### Q: 連接失敗？
A: 檢查Firebase配置是否正確，確認API金鑰和專案ID無誤。

## 💡 優化建議

1. **效能優化**：Firebase提供自動快取，載入速度更快
2. **安全性**：生產環境建議加入使用者驗證
3. **監控**：可在Firebase控制台查看使用統計
4. **備份**：Firebase提供自動備份功能

## 📞 技術支援

如遇到問題，請檢查：
1. 瀏覽器控制台的錯誤訊息
2. Firebase控制台的使用情況
3. 網路連線狀態

---

**完成遷移後，您的失物故事牆將由Firebase驅動，享受更穩定、更快速的服務！** 🎉
